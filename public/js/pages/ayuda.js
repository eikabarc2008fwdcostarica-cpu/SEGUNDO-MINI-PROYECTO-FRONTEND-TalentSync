"use strict";import{initLayout}from"../common/layout.js";import{$,esc,openModal}from"../common/ui.js";if(!await initLayout())await new Promise(()=>{});const articles=[{cat:"Primeros Pasos",title:"Configurar TalentSync",text:"Conoce el dashboard y la navegación principal."},{cat:"Gestión de Candidatos",title:"Organizar candidatos",text:"Crea, edita y conecta perfiles con procesos."},{cat:"Publicación de Vacantes",title:"Publicar una vacante",text:"Registra requisitos claros y verificables."},{cat:"Accesibilidad e Inclusión",title:"Navegación por teclado",text:"Usa Tab, Shift + Tab, Enter y Escape."},{cat:"Accesibilidad e Inclusión",title:"Contraste y lectores de pantalla",text:"TalentSync incluye foco visible, labels y regiones ARIA."},{cat:"Accesibilidad e Inclusión",title:"Redacción inclusiva",text:"Redacta requisitos profesionales sin atributos sensibles."}];let query="";function render(){const filtered=articles.filter(x=>!query||JSON.stringify(x).toLowerCase().includes(query));$("#page-content").innerHTML=`<section class="help-hero"><p class="eyebrow">Centro de Ayuda</p><h1>¿En qué podemos ayudarte hoy?</h1><p>Guías operativas y de inclusión.</p><div class="help-search"><input id="help-search" value="${esc(query)}" placeholder="Ej. ¿Cómo publicar una vacante urgente?" aria-label="Buscar artículos"></div></section><section class="help-grid">${["Primeros Pasos","Gestión de Candidatos","Publicación de Vacantes"].map(cat=>`<article class="help-category"><h2>${cat}</h2><p>${articles.find(x=>x.cat===cat).text}</p><button data-category="${cat}">Ver artículos</button></article>`).join("")}</section><section class="accessibility-center"><div><p class="eyebrow">Inclusión por diseño</p><h2>Centro de Accesibilidad e Inclusión</h2><p>Navegación por teclado, contraste, lectores de pantalla, redacción inclusiva y formularios accesibles.</p></div><div class="article-list">${filtered.map(x=>`<button data-article="${x.title}"><span><strong>${x.title}</strong><small>${x.text}</small></span></button>`).join("")}</div></section>`}$("#page-content").addEventListener("click",e=>{const b=e.target.closest("button");if(!b)return;if(b.dataset.category){query=b.dataset.category.toLowerCase();render()}if(b.dataset.article){const x=articles.find(y=>y.title===b.dataset.article);openModal(x.title,`<p class="quote">${esc(x.text)}</p>`,x.cat)}});$("#page-content").addEventListener("input",e=>{if(e.target.id==="help-search"){query=e.target.value.toLowerCase();render();$("#help-search").focus()}});addEventListener("page:search",e=>{query=e.detail.toLowerCase();render()});render();
