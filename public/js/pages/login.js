"use strict";
import{login,saveSession}from"../services/api.js";import{redirectIfAuthenticated}from"../common/auth.js";import{$}from"../common/ui.js";
redirectIfAuthenticated();$("#toggle-password").onclick=()=>{const field=$("#password"),show=field.type==="password";field.type=show?"text":"password";$("#toggle-password").textContent=show?"Ocultar":"Ver"};$("#login-form").addEventListener("submit",async event=>{event.preventDefault();const button=event.submitter,error=$("#login-error"),data=new FormData(event.currentTarget);button.disabled=true;button.textContent="Ingresando...";error.hidden=true;try{saveSession(await login(data.get("username").trim(),data.get("password")));location.href="dashboard.html"}catch(reason){error.textContent=reason.message;error.hidden=false}finally{button.disabled=false;button.textContent="Iniciar sesión"}});
