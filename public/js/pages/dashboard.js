"use strict";
import{initLayout}from"../common/layout.js";import{getResource}from"../services/api.js";import{$,esc,showError,showLoading}from"../common/ui.js";
if(!initLayout())await new Promise(()=>{});async function load(){showLoading();try{const[users,products,posts,todos]=await Promise.all([getResource("users",5),getResource("products",5),getResource("posts",5),getResource("todos",30)]),pending=todos.todos.filter(item=>!item.completed).length;$("#page-content").innerHTML=`<header class="page-head"><div><p class="eyebrow">Resumen general</p><h1>Dashboard</h1><p>Actividad general del equipo de reclutamiento.</p></div></header><section class="metrics">${[[users.total,"Total candidatos"],[posts.total,"Procesos activos"],[products.total,"Vacantes"],[pending,"Tareas pendientes"]].map(([value,label])=>`<article class="metric"><span class="metric-icon">•</span><strong>${value}</strong><small>${label}</small></article>`).join("")}</section><section class="dashboard-grid"><article class="panel"><div class="panel-head"><h2>Actividad reciente</h2><a href="postulaciones.html">Ver procesos</a></div><div class="activity">${users.users.map((user,i)=>`<div class="activity-row"><span class="activity-dot"></span><div><p><strong>${esc(user.firstName)} ${esc(user.lastName)}</strong> actualizó su proceso</p><small>Hace ${i+1} horas</small></div></div>`).join("")}</div></article><article class="panel"><div class="panel-head"><h2>Accesos rápidos</h2></div><div class="activity"><a class="btn btn--ghost" href="candidatos.html">Gestionar candidatos</a><a class="btn btn--ghost" href="vacantes.html?action=create">Publicar vacante</a><a class="btn btn--ghost" href="analisis-cv.html">Analizar CV</a></div></article></section>`}catch(error){showError(error,load)}}load();
