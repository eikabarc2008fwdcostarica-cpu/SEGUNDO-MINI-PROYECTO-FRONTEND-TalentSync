"use strict";import{getResource,getSession,internalRequest}from"../services/api.js";import{$,esc}from"./ui.js";
export async function toggleNotifications(){const panel=$("#notification-panel"),button=$("#notification-button");if(!panel.hidden){panel.hidden=true;button.setAttribute("aria-expanded","false");return}panel.hidden=false;button.setAttribute("aria-expanded","true");$("#notification-list").innerHTML='<p class="notification-loading">Cargando resumen...</p>';try{const session=getSession(),[comments,messageData]=await Promise.all([getResource("comments",5),internalRequest("/api/messages")]),unread=messageData.conversations.reduce((sum,item)=>sum+item.unread,0),items=[{href:"mensajeria.html",title:`${unread} mensajes no leídos`,text:"Abrir bandeja de entrada"},{href:"entrevistas.html?view=calendar",title:`${comments.comments.length} entrevistas próximas`,text:"Consultar calendario"}];if(session.role==="recruiter")items.push({href:"tareas.html",title:"Tareas del equipo",text:"Revisar prioridades"});$("#notification-count").textContent=unread||"";$("#notification-list").innerHTML=items.map(item=>`<a href="${item.href}"><strong>${esc(item.title)}</strong><small>${esc(item.text)}</small></a>`).join("")}catch(error){$("#notification-list").innerHTML=`<p class="form-error">${esc(error.message)}</p>`}}
