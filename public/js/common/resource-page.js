"use strict";
import{createResource,deleteResource,getResource,updateResource}from"../services/api.js";import{$,esc,openModal,closeModal,pagination,showError,showLoading,showToast}from"./ui.js";
const configs={candidatos:{resource:"users",key:"users",title:"Candidatos",subtitle:"Gestiona y descubre talento para tus procesos.",create:"Agregar candidato",fields:[["firstName","Nombre"],["lastName","Apellido"],["email","Correo","email"],["role","Rol"]]},vacantes:{resource:"products",key:"products",title:"Gestión de Vacantes",subtitle:"Administra oportunidades laborales activas.",create:"Publicar nueva vacante",fields:[["title","Vacante"],["brand","Empresa"],["category","Categoría"],["price","Salario","number"]]}};
export async function initResourcePage(kind){const config=configs[kind],target=$("#page-content");let data=[],query="",page=1;const perPage=8;async function load(){showLoading();try{const response=await getResource(config.resource,30);data=response[config.key];render();if(new URLSearchParams(location.search).get("action")==="create")form()}catch(error){showError(error,load)}}function filtered(){return query?data.filter(item=>JSON.stringify(item).toLowerCase().includes(query.toLowerCase())):data}function render(){const all=filtered(),rows=all.slice((page-1)*perPage,page*perPage);target.innerHTML=`<header class="page-head"><div><p class="eyebrow">TalentSync workspace</p><h1>${config.title}</h1><p>${config.subtitle}</p></div><button class="btn btn--primary" data-create>${config.create}</button></header><section class="panel"><div class="table-wrap"><table class="data-table"><thead><tr>${kind==="candidatos"?"<th>Candidato</th><th>Información / posición</th><th>Etiquetas</th>":"<th>Vacante</th><th>Empresa</th><th>Categoría</th><th>Estado</th><th>Salario</th>"}<th>Acciones</th></tr></thead><tbody>${rows.map(row).join("")}</tbody></table></div>${pagination(all.length,page,perPage)}</section>`}function row(item){if(kind==="candidatos")return `<tr><td><div class="person"><img src="${esc(item.image)}" alt=""><div><strong>${esc(item.firstName)} ${esc(item.lastName)}</strong><small>${esc(item.email)}</small></div></div></td><td>${esc(item.company?.title||item.role||"Profesional")}</td><td><span class="badge">${esc(item.company?.department||"Candidato")}</span></td><td><div class="row-actions"><button data-view="${item.id}">Ver</button><button data-edit="${item.id}">Editar</button><a href="mensajeria.html?candidateId=${item.id}">Mensaje</a><a href="analisis-cv.html?candidateId=${item.id}">Analizar CV</a><button data-delete="${item.id}">Eliminar</button></div></td></tr>`;return `<tr><td><strong>${esc(item.title)}</strong></td><td>${esc(item.brand||"Empresa cliente")}</td><td><span class="badge">${esc(item.category)}</span></td><td><span class="badge green">Activa</span></td><td>$${Number(item.price*100).toLocaleString("en-US")}</td><td><div class="row-actions"><button data-view="${item.id}">Ver</button><button data-edit="${item.id}">Editar</button><button data-delete="${item.id}">Eliminar</button></div></td></tr>`}function form(id){const item=id?data.find(x=>x.id===id):{};openModal(id?"Editar registro":config.create,`<form id="resource-form" data-id="${id||""}"><div class="form-grid">${config.fields.map(([name,label,type="text"])=>`<div class="field"><label for="field-${name}">${label}</label><input id="field-${name}" name="${name}" type="${type}" value="${esc(item[name]||"")}" required></div>`).join("")}</div><div class="modal-actions"><button type="button" class="btn btn--ghost" data-close-modal>Cancelar</button><button class="btn btn--primary">Guardar</button></div></form>`)}target.addEventListener("click",async event=>{const button=event.target.closest("button");if(!button)return;if(button.dataset.create!==undefined)form();if(button.dataset.edit)form(Number(button.dataset.edit));if(button.dataset.view){const item=data.find(x=>x.id===Number(button.dataset.view));openModal("Detalle",`<div class="detail-list">${Object.entries(item).filter(([,v])=>["string","number"].includes(typeof v)).slice(0,8).map(([k,v])=>`<div class="detail-row"><small>${esc(k)}</small><strong>${esc(v)}</strong></div>`).join("")}</div>${kind==="candidatos"?`<div class="modal-actions"><a class="btn btn--ghost" href="analisis-cv.html?candidateId=${item.id}">Analizar CV</a><a class="btn btn--primary" href="mensajeria.html?candidateId=${item.id}">Mensaje</a></div>`:""}`)}if(button.dataset.delete){const id=Number(button.dataset.delete);openModal("¿Eliminar registro?",`<p>La eliminación se simula en DummyJSON.</p><div class="modal-actions"><button class="btn btn--ghost" data-close-modal>Cancelar</button><button class="btn btn--danger" data-confirm="${id}">Eliminar</button></div>`)}if(button.dataset.confirm){try{await deleteResource(config.resource,Number(button.dataset.confirm));data=data.filter(x=>x.id!==Number(button.dataset.confirm));closeModal();render();showToast("Registro eliminado.")}catch(error){showToast(error.message,"error")}}if(button.dataset.page){page=Number(button.dataset.page);render()}});document.addEventListener("submit",async event=>{if(event.target.id!=="resource-form")return;event.preventDefault();const id=Number(event.target.dataset.id),values=Object.fromEntries(new FormData(event.target));try{if(id){const response=await updateResource(config.resource,id,values);data=data.map(x=>x.id===id?{...x,...values,...response}:x)}else{const response=await createResource(config.resource,values);data=[response,...data]}closeModal();render();showToast("Registro guardado.")}catch(error){showToast(error.message,"error")}});addEventListener("page:search",event=>{query=event.detail;page=1;render()});await load()}
